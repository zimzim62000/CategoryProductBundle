{% extends 'ZIMZIMCategoryProductBundle::layout.html.twig' %}

{% trans_default_domain 'ZIMZIMCategoryProduct' %}

{% block title %}
    {{ 'adminproduct.index.title'|trans }}
{% endblock title %}


{% block page %}
    <div class="row">
        <div class="small-10 columns">
            <h1 class="text-center headerpagehome">
                {{ 'adminproduct.index.title'|trans }}
            </h1>
        </div>
        <div class="small-2 columns">
            <h1 class="text-center headerpagehome">
                <a class="tiny button" href="{{ path('zimzim_categoryproduct_product_new') }}">
                    {{ 'button.create'|trans }}
                </a>
            </h1>
        </div>
    </div>

    {{ grid(grid) }}

{% endblock %}
